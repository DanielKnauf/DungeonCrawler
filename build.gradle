apply plugin: 'java'

version = '1.0-SNAPSHOT'

allprojects {
    repositories {
        google()
        jcenter()
        maven { url 'https://jitpack.io' }
    }
}

dependencies {
    implementation 'com.github.DanielKnauf:GameLogic:master'
    testImplementation 'junit:junit:4.12'
}