apply plugin: 'java'

allprojects {
    version '1.0'

    repositories {
        google()
        jcenter()
    }

    sourceCompatibility = 1.8
}

subprojects{
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    dependencies {

    }
}